/* selectors */

export const getProducts = ({ products }) => products;

/* action name creator */

/* action types */

/* action creators */

/* thunk creators */

/* reducer */

export const reducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
